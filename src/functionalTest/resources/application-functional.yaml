targetInstance: ${TEST_URL:http://localhost:8096}

spring:
  cache:
    cache-names: userInfoCache, accessTokenCache, systemTokenCache, legalRepTokenCache, legalRepOrgDeletedTokenCache, legalRepOrgSuccessTokenCache, citizenTokenCache
    caffeine:
      spec: expireAfterAccess=3600s
